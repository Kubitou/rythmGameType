services:
  compiler:
    build:
      context: .
      dockerfile: dockerfile

    volumes:
      - .:/app
      - node_modules:/app/node_modules

  web:
    image: nginx:alpine

    ports:
      - "8080:80"

    volumes:
      - .:/usr/share/nginx/html:ro

volumes:
  node_modules: